# Local environment and secrets
.env
.env.*
*.pem
*.key
*.p12
*.pfx
*.crt
*.cert
*token*
*cookie*
cookies.json

# Python / tooling
__pycache__/
*.py[cod]
.pytest_cache/
.venv/
venv/

# Private source inputs (follow.opml is committed: contains the live source list)
feeds/*.private.opml
feeds/private*.opml

# Optional private/generated digests
data/email-digest.json

# Local ops scripts & runtime logs (not shipped to the public repo)
scripts/patch-bole-skill.sh
scripts/generate_brief.sh
scripts/generate_brief.py
scripts/run_radar.sh
data/update.log
output/

# Local-only skill copy with machine-specific absolute paths (never commit)
skills/ai-news-radar-local/

# Local OS/editor noise
.DS_Store
.idea/
.vscode/
.gstack/

# Local pull log
logs/
